
（一）vector是什么？
      可以理解为一个与数组相同的数据类型
  
（二）使用vector必要的头文件：
      #include<vector>
      
（三）创建一个int型的名为obj的vector容器：
      vector<int>obj;
  
 (四) 往名为obj的vector容器里放入一个元素i：
      obj.push_back(i);
     
（五）求名为obj的vector容器的长度：
      obj.size();
      
 (六) 求名为obj的vector容器的第一个元素：
      obj.begin();
      
 (七) 求名为obj的vector容器的最后一个元素：
      obj.end();
  
 (八) 将vector型数据传入函数中：
        例：
       void Print(vector<int>obj){
            for(int i=0;i<obj.size();i++){
              cout<<obj[i]<<",";
            }
           cout<<"\n";
        }
